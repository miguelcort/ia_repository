# Clasificacion de imagenes

Softmax, cross-entropy, top-k accuracy, confusion matrix, augmentation:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```