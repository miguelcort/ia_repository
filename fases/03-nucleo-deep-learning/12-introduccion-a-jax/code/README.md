# Introduccion a JAX

jax.numpy, grad, jit, vmap:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```

Requiere `pip install jax jaxlib` (se ejecuta aunque no este disponible).