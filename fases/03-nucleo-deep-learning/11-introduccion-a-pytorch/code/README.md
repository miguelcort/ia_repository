# Introduccion a PyTorch

Tensor, autograd, nn.Module, optim, loss, loop de entrenamiento:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```

Requiere `pip install torch` (se ejecuta aunque no este disponible).