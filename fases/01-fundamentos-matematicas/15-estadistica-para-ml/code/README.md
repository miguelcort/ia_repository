# Estadistica para ML

Media, mediana, varianza, std, correlacion Pearson/Spearman, t-test:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```