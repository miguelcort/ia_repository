# Recuperacion de imagenes y metric learning

Encoder, indice, busqueda por similitud, recall@k, mAP:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```