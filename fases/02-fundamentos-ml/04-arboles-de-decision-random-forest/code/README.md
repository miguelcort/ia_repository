# Arboles de decision y Random Forest

Demo: entropia, ganancia de informacion, mejor split:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```