# Vision Transformers (ViT)

Patch embedding, positional encoding, self-attention, classification head:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```