# Sesgo, varianza y curva de aprendizaje

Demo: sesgo-varianza via bootstrap y curva de aprendizaje:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```