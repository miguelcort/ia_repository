# Deteccion de actividad de voz y turn-taking

VAD energia, silero VAD mock, endpointing:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```