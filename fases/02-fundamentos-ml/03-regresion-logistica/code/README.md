# Regresion logistica y clasificacion

Demo: sigmoid, ajuste por GD, prediccion y accuracy:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```