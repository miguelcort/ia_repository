# Espectrogramas y caracteristicas mel

FFT, STFT, mel scale, mel filterbank, mel-spectrogram:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```