# Arquitectura Whisper y fine-tuning

Chunking 30s, mel-spectrogram 80 mels, decoder mock, special tokens:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```