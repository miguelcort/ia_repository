# Schedules de learning rate

Constante, step, exponential, cosine, warmup+cosine, reduce-on-plateau:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```