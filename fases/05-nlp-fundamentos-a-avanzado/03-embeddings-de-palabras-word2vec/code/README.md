# Embeddings de palabras con word2vec

Skip-gram, negative sampling, analogia, similitud coseno:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```