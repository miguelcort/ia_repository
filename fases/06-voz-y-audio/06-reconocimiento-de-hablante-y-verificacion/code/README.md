# Reconocimiento de hablante y verificacion

x-vector mock, cosine similarity, verification, identification, EER:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```