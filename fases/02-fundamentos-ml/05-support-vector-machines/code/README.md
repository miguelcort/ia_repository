# Support Vector Machines (SVM)

Demo: kernels (lineal, RBF), margen geometrico, hinge loss:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```