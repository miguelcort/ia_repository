# Gestion de datos

Carga, valida y genera un manifiesto versionable de un CSV:

```bash
cd code
python3 main.py ruta/al/dataset.csv
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```

O desde la raíz del repo:

```bash
python3 scripts/probar_lecciones.py --quiet
```
