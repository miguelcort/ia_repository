# Optimizacion convexa

Demo: deteccion de convexidad via Hessiana y gradiente descendente en funcion convexa:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```