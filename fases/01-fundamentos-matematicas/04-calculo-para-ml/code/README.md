# Calculo para ML: derivadas y gradientes

Derivada de polinomios, derivada numerica y descenso por gradiente:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```
