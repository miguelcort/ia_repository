# Transformaciones y valores propios

Demo de rotacion, escalado, autovalores y diagonalizacion:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```
