# Clonacion de voz y conversion

Speaker encoder, voice conversion, similarity, consistency:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```