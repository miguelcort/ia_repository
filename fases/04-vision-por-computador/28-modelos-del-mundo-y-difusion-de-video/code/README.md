# Modelos del mundo y difusion de video

Spacetime patches, positional encoding 3D, world model step:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```