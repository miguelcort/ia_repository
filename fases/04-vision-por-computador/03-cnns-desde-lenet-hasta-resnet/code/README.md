# CNNs: de LeNet a ResNet

Bloques conv+BN+ReLU, residual, receptive field, conteo de params:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```