# SAM3 y segmentacion de vocabulario abierto

Image encoder, prompt encoder, mask decoder, IoU:

```bash
cd code
python3 main.py
```

Ejecuta los tests:

```bash
python3 -m unittest discover -s tests -v
```